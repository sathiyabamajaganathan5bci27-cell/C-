#include <iostream>

using namespace std;

int main() {
    // Declare variables to store the three integers
    int a, b, c;
    
    // Read the three space-separated integers from stdin
    cin >> a >> b >> c;
    
    // Calculate the sum
    int sum = a + b + c;
    
    // Print the sum to stdout followed by a newline
    cout << sum << endl;
    
    return 0;
}
